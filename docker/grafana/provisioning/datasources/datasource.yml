apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    url: http://prometheus:9090 # ⬅️ Grafana 连接 Prometheus 的地址 (服务名:端口)
    access: proxy
    isDefault: true
    version: 1
    editable: true
